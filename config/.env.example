# WHOOP OAuth Configuration
# Run: uv run python scripts/get_token.py to get all these values

# Access token (required)
WHOOP_ACCESS_TOKEN=

# Auto-refresh configuration (recommended)
# With these set, tokens will automatically refresh when expired
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REFRESH_TOKEN=
