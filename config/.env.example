# WHOOP OAuth Configuration

# Step 1: Add your app credentials from WHOOP Developer Dashboard
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=

# Step 2: Run `uv run python scripts/get_token.py` and copy the output below
WHOOP_ACCESS_TOKEN=
WHOOP_REFRESH_TOKEN=
